import { describe, it, expect } from "vitest";

describe("MCP server", () => {
  it.todo("registers verify_email tool with correct input schema");
  it.todo("returns structured VerificationResult for valid input");
  it.todo("rejects missing or non-string address");
});
